# 校验器变更登记

> 冻结清单里的文件（`work/physkit/**`、`work/qc.py`、`work/net_guard.py`、`work/orchestrator.py`）
> 一旦改动，必须在这里登记：**改了哪个文件、第几行、为什么必须改、对已有内容的影响**。
> 制度依据：`参考文献/13` 第 2 条 ——「放松检查比写错内容更危险」。

---

## 2026-09-19 · 成品体积上限：300 KB → 2 MB（仅防呆）

**改动人**：质检方（质检方）

**授权来源**：需求方原话 ——「放宽，大小不重要，重要的是成品的效果。不出 bug 就行。」

### 改了哪些文件

| 文件 | 位置 | 改动 |
|---|---|---|
| `work/qc.py` | `check_output()` 内体积判定 | `> 300` → `> 2048`；文案改为「2 MB 防呆上限」 |
| `work/build_site.py` | 结尾体积判定 | `> 300` → `> 2048`；原「需要精简内容」的提示删除 |
| `work/build_site.py` | 文件头注释 | 「硬约束…小于 300 KB」→「已放宽」 |
| `AGENTS.md` | 第 3 节第 6 条 | 「体积预算 < 300 KB」→「已放宽，2 MB 防呆」 |
| `参考文献/13_交接给Codex_任务与验收标准.md` | 硬约束第 3 条、成品一致性一节 | 同步去掉 300 KB |
| `参考文献/11_项目流水线_可外包的项目.md` | 「其他约束不变」一段 | 同步 |
| `work/_进度.py` | 成品体积提示文案 | 同步（质检方的看进度小工具） |

### 为什么必须改

体积**不是正确性检查，是形态约束**。做到第 4 章时成品已达 304~314 KB，
执行方为了压进 300 KB 开始**反复精简内容**，直接后果是：

- 覆盖门槛 **2 处未达标**：`mom-01` 缺「量纲抓不到、只能靠数值代入」的陷阱，
  `mom-05` 的一致性检查为 0；
- 事件流里能看到它反复 `量体积 → 改 kb json → build_site → 再量` 的空转。

后面还有 2 章要加。若维持 300 KB，只能持续牺牲内容深度 ——
这与项目目标（**作品质量 > 文件大小**）相悖，故按需求方指示放宽。

### 对已有内容的影响

- **对正确性零影响**：指纹、覆盖门槛、独立复算、成品数量一致性、人工抽样单
  全部逻辑未动；改的只是一个数值上限和几处文案。
- 已交付的前三章不受影响（250.5 KB 本来就没碰线）。
- 新的 2 MB 上限仅防失控膨胀；正常交付（预估 400~600 KB）碰不到。

### 残留的已知不一致（有意保留）

- `work/physkit/expr.py` 注释里仍写着 300 KB —— 那是「为什么用 MathML 而不是 KaTeX」
  的论证（KaTeX 1 MB+ vs MathML 零体积）。**该论证在新上限下依然成立**，
  且 `physkit/**` 属冻结范围不宜轻改，故保留原样。
- `参考文献/06`、`07`、`09`、`10` 等**前期调研文档**里也有 300 KB 字样 ——
  那是当时的设计记录，属历史存档，**不改**（改了等于篡改历史）。
  以本文件与 `AGENTS.md` 第 3 节为准。

### 基线

改动后已重冻：`python work/qc.py --freeze`

---

## 2026-09-19 · 编排器：全部批次完成后直接收工（不再空跑烧额度）

**改动人**：质检方（质检方）　**授权来源**：需求方「codex 可能用量不够」（2026-09-19 04:43）

### 现象
六章在 04:42 全部交付完毕后，循环**并没有停**：
- 第 17~23 轮全是 `批次 None`，Codex 每轮 **0 分钟退出、退出码 1**，
  对账 5 个问题、质检未过 → **连续失败 7 轮 → 停机**；
- 计划任务每 30 分钟又拉起一次，于是「停机 → 拉起 → 再失败」循环。
- 错误日志里的真因：`You've hit your usage limit ... try again at 6:14 AM.`
  —— 额度在 04:42 前后耗尽，这些轮次全是**无效重试**。
- 状态文件因此长期显示「已停机 / 连续失败 7」，看着像项目坏了，
  实际只是**没活可干还在派活**。

### 改动
`work/orchestrator.py` 主循环，`batch = next_batch(plan)` 之后新增：

```python
if batch is None:
    state.update({"阶段": "全部完成",
                  "停机原因": "六批全部已完成（质检+对账通过），无待做批次 —— 正常收工",
                  "最后更新": now()})
    write_json(STATE_FILE, state)
    log("批次计划已无待做批次 —— 全部完成，正常收工（不调用 Codex）")
    stop_reason = "全部批次已完成"
    break
```

原来 `batch is None` 时会走 `build_prompt` 生成一个「收尾批次」派活，
让 Codex **再跑一遍 validate/build/qc** —— 每 30 分钟重复一次，白烧额度。

### 对已有内容的影响
- **对内容零影响**：只改了「没活可派时怎么办」，不碰任何校验逻辑。
- 副作用：Codex 不再写「全部完成」的收尾报告（那份报告现在由质检方的汇报代劳）。
- 若将来扩批次（在 `work/批次计划.md` 里加第 7~N 批），`next_batch` 会正常返回新批次，
  循环照常推进 —— 不受本次改动影响。

### 验证
- `python work/orchestrator.py --rounds 1` → 输出
  「批次计划已无待做批次 —— 全部完成，正常收工（**不调用 Codex**）」，
  且全程没有启动 Codex ✓
- 重冻基线：`orchestrator.py` → `0c7b188b29d49391`

### 配套处置
- 计划任务 `CodexKBLoop` 已 **Disable**（活干完了，避免继续空转）；
  将来要再跑（例如扩章节）时 `Enable-ScheduledTask -TaskName 'CodexKBLoop'` 即可。

---

## 2026-09-19 · 正文公式排版还原 + 新增「内容自洽性」检查（补掉一个真实盲区）

**改动人**：质检方（质检方）

**起因**：需求方验收成品时发现 ——「它里面的公式使用的字符并非我们常用的，
学习中绝不会是这种复杂的」。典型例子：`T = 2pi sqrt(L/g)`、
`x = A cos(omega t + phi)`。

### 现象与根因

成品**公式框**里的式子是对的（走 MathML），问题出在**正文散文**：
作者写作时顺手敲了机器写法。全库扫下来 **174 处字段、313 个片段**。

拆开是两类，第二类更要紧：

1. **显示问题** —— 正文里的公式从来没做过排版。`build_site.py` 的 `paras()`
   只做 HTML 转义，不做任何公式处理。
2. **一致性问题** —— 正文公式**不参与任何校验**。校验器只比每条公式的
   `expr` 字段，正文写什么它一概不看。于是 osc-06 出现了
   「定义正文写 `v = lambda f`，符号表却把波长定义成 `wavelength`」——
   同一个物理量在一张卡片里两个名字，**六道检查一道都没抓到**。

### 改了哪些文件

| 文件 | 改动 |
|---|---|
| `work/prose_math.py` | **新增**。把正文里的机器写法还原成教材写法（`2pi sqrt(L/g)`→`2π √(L/g)`、`F_合`→`F<sub>合</sub>`、`A^2`→`A²`）。纯查表 + 正则，不含物理判断，幂等。自带 21 条自测。 |
| `work/build_site.py` | 正文渲染由 `E()`（纯转义）改为 `prose()`，覆盖定义 / 物理意义 / 推导 / 例题 / 常见错误 / 符号表 / 校验记录。**公式框的 `expr` 一个字符都没动。** |
| `work/qc.py` | 新增第七项检查「内容自洽性」；并修掉保留字 `lambda` 无法求值的缺陷（见下） |
| `work/kb/06_oscillation_wave.json` | `wavelength` → `lambda`（20 处），与 osc-05 统一，页面显示为 λ |

### 为什么必须改

- **显示**：学习者看到的是 `2pi sqrt(L/g)`，教材上从来不这么写。
- **一致性**：正文公式不进校验 = 一个没人看的角落。这次是命名不一致，
  下次可能是正文里的公式抄错系数 —— 同样没人抓。

### 修 qc.py 的保留字缺陷（关键）

把 osc-06 的符号统一成 `lambda` 之后，独立复算立刻报 **3 处 `invalid syntax`**。

根因：`physkit/expr.py` 的 `GREEK` 词表里明确写着 `"lambda": "λ"`
（生产解析器是自己写的，能处理）；而 `qc.py` 的独立复算**刻意**改用
Python 原生 `ast`，`lambda` 是 Python 保留字，**在语法层就被拒绝**，
连环境都轮不到查。

这不是内容的错，是**质检工具自己的缺陷**：独立复算器无法校验含合法符号
`lambda` 的公式，等于把这类公式整批漏掉。而且它表现为「独立复算失败」，
看多了会被当成噪音忽略掉 —— 那比不报更危险。

修法：求值前把保留字换成安全名（`lambda` → `_lam_`），取名字时再还原。
**只改名字，不动任何计算逻辑**，独立复算「两条代码路径互不相同」的原则不变。

修完：独立复算回到 **145 项全部相符**。

### 新增检查：内容自洽性（qc.py 第七项）

规则：**正文里用到的量，本卡片的符号表里应该能找到。**

- 符号名拿英文单词充数（如 `wavelength`）→ **硬问题，拦**；
- 正文引用了本卡片没定义的量（推导时顺手用到邻近知识点的 θ、ω）→
  **软提示，不拦**，列出来给人判断。

当前结果：**0 硬 / 4 软**。4 条软提示分别是 ene-03 的 θ、cir-03 的 a_c、
osc-03 的 θ 与 ω —— 都是推导时引用了相邻知识点的量，不影响正确性。

### 对已有内容的影响

- **对正确性零影响**：`expr` 字段一个字符没动；符号表只改了 06 章那一个名字。
- 成品体积 441.4 KB → 444.7 KB（多出来的是 `<sub>` 标签），远低于 2 MB 防呆上限。
- 493 项内容校验、145 项独立复算，改动后**仍然全过**。

### 验证

- `python work/build_site.py` → 493 / 493 通过
- `python work/qc.py --check` → 指纹 0 / 覆盖门槛 0 / 独立复算 0（145 项）/
  成品一致性 0 / 质量扫描 0 / 内容自洽性 0 硬 4 软 → ✔ 通过
- 正文残留扫描：机器写法从 98 处降到 **1 处**，且那 1 处是符号表 MathML
  里的变量名，不是正文

### 基线

改动后已重冻：`work/qc.py` → `6f5ec5f3a27be5d0`（其余冻结文件未变）

### 待办（不属本次范围，留给执行方）

- 4 条软提示可考虑把 θ / ω / a_c 补进相应知识点的符号表（可选，不拦）
- osc-06 的 `lambda` 归一后，与 osc-05 的命名已经一致

---

## 2026-09-19 · 网络体检：探测顺序对齐到「真正干活的那条通道」

**改动人**：质检方（质检方）　**授权来源**：需求方选定「先补内容（电磁学等）」，
开跑第 7~21 批之前的开跑前加固

### 改了哪个文件、哪一行

| 文件 | 位置 | 改动 |
|---|---|---|
| `work/net_guard.py` | `CANDIDATE_PORTS` | `[10808, 10809, 1080, 7890, 7897]` → `[7897, 10808, 10809, 1080, 7890]`；仅**提升 7897 到首位**，其余相对顺序不变 |

### 为什么必须改

这是「体检通道 ≠ 干活通道」的洞，**靠一次巧合活着**。

- **体检**走 `net_guard.evaluate()`，它按 `CANDIDATE_PORTS` 顺序试，
  老顺序下 **10808（v2rayN）排第一**；
- **干活**走 `orchestrator.py` 的 `CONFIG["代理"] = http://127.0.0.1:7897`（SakuraCat）。

两条通道**互不知情**。于是 SakuraCat 一掉而 v2rayN 还活着时：
体检探 10808 → 通 → 报「网络正常」→ 编排器放心派活 →
**Codex 走 7897 连不上 chatgpt.com** → 一轮轮空转 → 连续失败 4 轮 → 停机。

实测（2026-09-19 07:32）两个端口**碰巧都活着**，且 `evaluate()` 确实挑了 10808，
`7897` 与 `10808` 各自直连 chatgpt.com 均返回 `200 Connection established` ——
洞因此从未暴露。但 7897 是需求方桌面上常开的客户端提供的，
**随时可能退出**；后面还有 15 批要无人值守地连跑，不能把成功率押在巧合上。

### 为什么选「改顺序」而不是「加自动切换」

另一条路是让编排器按体检结果**动态改写**代理端口（SakuraCat 掉就切 v2rayN）。
**故意不选**：按实测，v2rayN(10808) 的建连失败率是 15%（20 次请求失败 3 次，
含两次 20 秒 TLS 握手死等），SakuraCat(7897) 是 0%。Codex 靠 WebSocket 长连接，
切到一条 15% 失败率的通道上，**"能连上"只是把显性失败换成隐性断续** ——
2026-09-18 夜里连停 5 轮零产出正是这么来的。

所以取**「宁可等，不要静默降级」**：体检如实反映干活通道，
不通就报不通，编排器去等网络恢复（本来就是设计好的自愈路径），
不烧额度、也不把内容生产押在一条烂线路上。

### 对已有内容的影响

- **对正确性零影响**：不碰任何内容、任何校验逻辑。
  变的只是「体检先看哪个端口」，是一个**探测顺序**。
- 覆盖面还扩大了：改前只探到 10808 也算通，改后 10808/10809/1080/7890
  仍全在候选里，只是**优先级降后**。任一通道活着都还能被探到，
  只是需要「干活通道优先」时才先探 7897。
- 与 `CONFIG["代理"]` 的一致性：现在两者默认同为 7897。
  ⚠ **耦合提醒**：日后若 `orchestrator.py` 里换了代理端口，
  `CANDIDATE_PORTS` 首位要跟着换（两处已在各自注释里写了互指提醒）。

### 验证

- `python work/net_guard.py` → 在听端口 `[7897, 10808]`，
  **可用通道 7897（http）**，结论 ✔ 网络正常（改前报的是 10808）
- `python work/qc.py --check`（未重冻）→ **指纹问题 1**（如实报出 net_guard.py 被改动）
  —— 说明指纹机制在正常工作，不是绕过
- 重冻后复跑见「基线」一节

### 基线

改动后已重冻：`work/net_guard.py` → 见 `质检基线.json`。
本次**只动这一个冻结文件**，其余六个冻结文件指纹不变。

---

## 2026-09-19 · 编排器：把「额度用尽」从生产失败里摘出来（资源故障 ≠ 内容失败）

**改动人**：质检方（质检方）　**授权来源**：需求方指出「实际上是 gpt 用量的问题，
它的用量 5 小时重置一次」（2026-09-19 11:29）

### 现象与真因

07:33 开跑后第 7~16 批一路顺产（10 批 / 92 分钟）。**09:19:49 起骤停**，
之后每 30 分钟醒一次、每次撞同一堵墙，连续失败累到 7 停机。

读 `logs/轮次_NNN_codex输出.txt` 的**报错原文**（不能只看症状 ——
「额度用尽」「服务端满载」「被掐死」三者的表面症状几乎相同：

```
{"type":"error","message":"You’ve hit your usage limit. Upgrade to Pro … try again at 11:44 AM."}
{"type":"turn.failed","error":{"message":"…try again at 11:44 AM."}}
```

报错原文**自己就写着几点重置**（11:44），与需求方说的完全一致。

### 为什么必须改（不是"看着烦"，是它会把新章节锁死）

编排器原来把额度用尽一律当成「Codex 没正常收工」的生产失败，于是连锁出三件事：

1. **伪造对账冲突** —— 仍拿**上一轮的** `work/交接单.json` 去跟本轮起始时间比，
   条条判「虚报」，还附一条「交付文件本轮没被改动 —— 这一批实际上没产出」。
   Codex 压根没参与，却被判"对账没过"。
2. **下一轮被写成硬禁令** —— `build_prompt()` 把这类实打实的冲突翻译成
   **「先处理这个：上一轮没对上账 …… 这一轮不要开新章节」** →
   **新章节永远开不了工**，循环原地打转。
3. **计入「连续失败」→ 停机** —— 看着像"项目坏了"，其实只是要等窗口重置；
   而且每次都要人手动去洗状态。

> 这一条和 07:33 修的那次是**同一个坑的第二种触发方式**：
> 上次是「没活可干」，这次是「额度用完」，都会经 `上轮对账问题` → `build_prompt()`
> 把新章节锁死。上次只清了状态（治标），这次改成机制（治本）。

### 改了哪些文件、哪几处

只动 `work/orchestrator.py` 一个文件，四处（互相关联，缺一不可）：

| # | 位置 | 改动 |
|---|---|---|
| 1 | 新增 `QUOTA_KEYS` / `detect_quota(hb, human_file)` | 从事件流的 `last_err` **和**人读流水两处认「usage limit」，顺带用正则抠出「几点重置」 |
| 2 | `run_codex()` 收尾处 | 把额度判定**放在「退出码 != 0」之前** —— 额度问题的现象正好是「退出码 1 + 0 分钟」，先走下面那条就会被写成生产失败 |
| 3 | 主循环「对账」一步 | `if ok:` 才 `verify_handoff()`；否则**不重算、保留上一轮原值** |
| 4 | 主循环 `if passed / else` 之间 | 插入 `elif "额度用尽" in note:` 分支：不计连续失败、不置「修错中」、置「等额度窗口」后 `break` |

### 几个刻意的设计决定

- **`detect_quota()` 返回的文案里不许出现「退出码 / 掐掉 / 超时 / 重连」等词。**
  因为 `build_prompt()` 见到那些词就会写「上一轮你没能正常收工，本轮算重来」，
  让 Codex 白跑一次 `validate.py`。额度问题不需要它"重来"，只需要等窗口。
  文案是 `Codex 额度用尽（用量窗口用完，报错原文说 11:44 AM 重置）`。
- **额度分支 `break` 而不是继续。** 否则同一轮里会连撞 6 次（一次定时跑 6 轮）。
- **不引入"自动等到 11:44"** —— 报错里的重置时间只是**给人看的**，
  不拿它做调度（时区/格式一变形就会算错）。靠定时任务每 30 分钟自然重试即可。
- **对账仍然对「正常收工但没产出」严格**：那是真正要抓的（8-19 第一轮就是这种），
  本次改动**没有放松**它，只是不再让"没开工"的轮次冒充"对账冲突"。

### 对已有内容的影响

- **对已交付的第 1~16 批零影响**：改的都是「失败路径」的分支，
  正常路径（`ok=True` → 对账 → 质检 → passed）一个字没动。
- **对内容正确性零影响**：不碰 `physkit/**`、不碰 `qc.py` 的任何判定。
  校验强度不变，只是失败分类变准了。

### 验证

1. `python -m py_compile work/orchestrator.py` → 退出码 0
2. **拿真实日志做单元测试**（4 例，全部相符）：

   | 用例 | 数据来源 | 期望 | 结果 |
   |---|---|---|---|
   | 真·额度用尽 | 轮 40 的 `last_err` | 认出 + 抠出 11:44 AM | ✔ |
   | 真·网络断流 | 轮 39 的 `Reconnecting… tls handshake eof` | **不**误判为额度 | ✔ |
   | 真·正常收工 | 轮 33 的流水 | **不**误判为额度 | ✔ |
   | 额度只出现在人读流水里 | 轮 40 的 `codex输出.txt` | 仍能认出 | ✔ |

   （专门测「不误判」两例 —— 把网络问题误认成额度，会让它白等一个窗口，比不认更糟。）
3. `python work/qc.py --check` → 见「基线」一节

### 基线

改动后已重冻 `work/orchestrator.py`（新指纹见 `质检基线.json`）。
备份：`work/_backup/orchestrator_改额度识别前_20260919_113226.py`
（改前指纹 `0c7b188b29d49391`）。

---

## 2026-09-19 · 体检判据改成「真握手」+「体检通道 = 干活通道」

**改动人**：质检方（质检方）　**授权来源**：需求方指出额度是 5 小时窗口（11:29）；
额度恢复后又实测到出口劣化（11:52）

### 现象：额度恢复后，循环连撞三轮

11:44 额度窗口回来后重启循环，**轮 43/44/45 全部失败**，日志是：

```
Codex 网络重连反复失败（最近 5 分钟刷了 3 条：Reconnecting... 4/5 (request timed out)）
```

而同一时刻 `net_guard.py` 体检**报「通」**，两条隧道各自 CONNECT 也是 200。
**体检说没事，干活却撞墙** —— 又是「体检 ≠ 干活」，但这次的成因和上午那个不同。

### 真因（两层，都用实测说话）

**第一层：老的体检判据本身会骗人。**

原来「代理回了 `200 Connection established` 就算通」。但**代理可以在本地就把
CONNECT 答掉、根本不拨上游**。同一条出口、同一天，两种判据的结论天差地别：

| 判据 | 结果 |
|---|---|
| 只发 CONNECT | 20/20 全通，**平均 0.01s**（"完美"） |
| 真握手 + 真读响应 | **15/20（25% 失败）**，平均 2.9s，最慢 11.6s（全是 TLS 握手超时） |

**旧判据会给一条烂隧道盖上"通"的章。**

**第二层：哪条通道好，一天之内翻过两次。**

| | 上午实测 | **下午实测** |
|---|---|---|
| 7897 SakuraCat | **20/20、0.43s** → 定为首选 | **15/20（25%）、2.9s、最慢 11.6s** |
| 10808 v2rayN | 失败 3 次（15%），含两次 20 秒死等 | **20/20、1.08s** |

→ 把"好的那条"写死在配置里，等于**每次节点一变就要人来改一次**，
而无人值守的循环恰恰没人在。

### 改了哪些文件、哪几处

| 文件 | 位置 | 改动 |
|---|---|---|
| `work/net_guard.py` | `import` | 加 `ssl` |
| `work/net_guard.py` | `CANDIDATE_PORTS` | `[7897,…]` → `[10808, 7897, 10809, 1080, 7890]`（按最近实测排序；注释里写明"顺序只是倾向，实测才是机制"） |
| `work/net_guard.py` | `http_connect_test()` | CONNECT 成功后**必须再跟目标完成 TLS 握手**才算通；失败信息区分「代理拒绝 / 握手超时 / 握手失败」 |
| `work/orchestrator.py` | 主循环「网络体检」之后 | **新增：把体检选中的端口直接写成本轮 `CONFIG["代理"]`**（"体检通道 = 干活通道"） |
| `work/orchestrator.py` | `CONFIG["代理"]` | `7897` → `10808`，并注明**这一项已退化为兜底默认值**，真正生效的是体检结果 |

### 为什么这么设计（而不是"再改一次端口"）

只改端口是**治标**：节点一劣化又要人来改。真正的机制是两条：

1. **判据要真** —— 体检必须真的跑通一次数据（TLS 握手），否则"检查通过"是假安心。
   代价是多花几百毫秒，换来的是「体检说通」这句话重新变得可信。
2. **体检结果要被采用** —— 实测哪条通，本轮就用哪条。
   这样「体检说通」和「Codex 能用」**由构造保证是同一件事**，不再靠人同步两处配置。

> 这与上午那次「只改探测顺序」是同一问题的递进：上午只把顺序对齐了干活端口，
> 但**判据还是假的、配置还是写死的**。现在两处都补上了。

### 对已有内容的影响

- **对内容与正确性零影响**：不碰 `physkit/**`、不碰 `qc.py` 的任何判定。
- **对已交付 1~16 批零影响**：改的是「开跑前选通道」和「体检判据」，
  内容校验与对账的严格度**一分未放松**。
- 若两条通道都不可用，`evaluate()` 如实报不通 → 编排器走原本就设计好的
  「等网络」路径（不会静默降级到一条烂通道上）。

### 验证

- `py_compile` 两个文件均通过
- 新判据实测：`7897` → 隧道可用（TLS 握手 0.82s）、`10808` → 隧道可用（0.65s）；
  `evaluate()` 选出 **10808**（与 20 次基准的结论一致）
- `python work/qc.py --check` → 见「基线」一节
- ★★ **决定性验收（不看体检报绿，看"能不能真的产出"）**：
  **同一个轮次号 45**，换通道前连撞三次 `Reconnecting... (request timed out)`；
  换到 10808 后重跑 —— **`Codex 正常收工（用了 3 分钟，37 个事件）`**，
  对账通过、轮后质检全绿，**第 17 批交付**（`《批次计划》已把第 17 批标为已完成`）。
  随后轮 46 自动开工第 18 批。
  → 这才叫"修好了"：**同一条输入、同一个轮次，从失败变成成功**。

### 基线

改动后已重冻 `work/net_guard.py` 与 `work/orchestrator.py`
（新指纹见 `质检基线.json`）。备份：
`work/_backup/net_guard_改真握手前_20260919_1157.py`、
`work/_backup/orchestrator_改通道采用前_20260919_1157.py`。

### 附：本次停机时的一个环境事实

用 `Stop-ScheduledTask` 掐掉编排器后，`.loop.lock` 会**残留**（编排器没机会清理）。
编排器本身有锁过期判定，下次开跑会自行处理；但若发现循环"启动了却不动"，
先看这把锁。

另外查明：任务管理器里常驻的 `codex.exe … app-server` 是
**ChatGPT 桌面版（ChatGPT.exe）拉起的**，不是本循环的进程。
别把它当孤儿杀掉 —— 杀掉会被 ChatGPT.exe 立刻重新拉起（实测连杀两次都复活）。

---

## 2026-09-19 · 渲染层：把漏到页面上的内部单位代号（`T_` / `ohm`）还原成教材写法

**改动人**：质检方（质检方）　**发现方式**：需求方要求「查 bug」→ 人工复核第 7~17 批时发现
（详见 `outputs/人工复核意见_第7-17批.md`）

### 现象（读者可见）

页面上，磁感应强度的单位显示成 **`T_`**，电阻的单位显示成 **`ohm`**。
教材里应当是 **`T`（特斯拉）** 与 **`Ω`（欧姆）**。

```html
<td class="sym-unit">T_</td>          ← 符号表，19 处
<span …>B 标注单位 'T_'（M·T⁻²·I⁻¹）…  ← 检查说明里也漏，共 24 处
```

### 根因

`work/physkit/quantity.py` 的单位表里，**特斯拉的键写作 `"T_"`** ——
因为 `T` 已被「时间」的量纲代号占用，只能加下划线避让。
**那是校验器的内部命名，本不该出现在给人看的地方**；
但内容里照抄了这个键（`"unit": "T_"`），显示层又不认识它，于是原样印出。

> 这是**同一类问题的第三次发作**：机器内部记号漏进了「给人看」的地方。
> 前两次分别是：① 正文公式写成 `2pi sqrt(L/g)`；
> ② 正文符号与符号表用两个名字（`lambda` / `wavelength`）。
> 前两次都补了机制（`prose_math.py` 排版还原、`qc.py` 内容自洽性检查），
> 这一处是它们的**漏网**。

### 改了哪个文件

**只改 `work/prose_math.py` 一处**，新增 `_ALIAS` 规则表并在 `render()` **最前面**执行：

| 规则 | 说明 |
|---|---|
| `ohm → Ω`（整词匹配） | 欧姆 |
| `T_ → T`（**带前瞻断言**） | 特斯拉 |

★ **为什么必须带前瞻断言、不能用简单 replace**：
正文里 `T_0` 是「**周期 T₀**」，不是特斯拉 —— `1/T_0` 若被改成 `T0` 就是**制造新 bug**。
规则限定为「后面不再接字母 / 数字 / 下划线」，即只替换**独立成单位**的那种 `T_`。

★ **为什么放在 `render()` 最前面**：必须在下标规则之前判断，否则 `T_` 会先被当成
「T 带空下标」处理，后面的规则就再也认不出它。

### 为什么只改一处就够

单位单元格与检查说明**都走 `prose()`**（`build_site.py` 里 `prose(s["unit"])`、
检查明细同样走 `prose()`），所以在 `render()` 里加一步即覆盖全部出口，
**不需要改 `build_site.py`**。

### 对已有内容的影响

- **对正确性零影响**：只改「一个单位字符串怎么写」，不涉及任何物理量、任何校验判定。
- **不影响任何校验结论**：`expr` 字段与 `unit` 字段的机器值一个字没动，
  改的纯粹是**渲染成 HTML 的那一刻**。
- `physkit/quantity.py` 的 `T_` 键**有意保留不动** —— 它作为内部代号是合理的，
  且它是冻结文件；错在「内部代号被当成公开值用」，修在出口即可。

### 验证

- `python work/prose_math.py` → 自测 **29 / 29 通过**（原 21 条 + 新增 8 条，
  其中 **2 条是反向保护**：`f_0 = 1/T_0` 与 `T_0^2 = 4pi^2 L/g`，专门盯误伤）
- `python work/build_site.py` → 结构 0 错、**物理检查 1260 项 100% 通过**，成品 1154.0 KB
- 成品实测：

  | 项 | 改前 | 改后 |
  |---|---|---|
  | 符号表显示 `T_` | 19 | **0** |
  | 符号表显示 `ohm` | 20 | **0** |
  | 符号表显示 `T` / `Ω` | 0 / 0 | **19 / 20** |
  | 全文独立 `T_` | 24 | **0** |
  | 全文 `ohm` | 29 | **0** |
  | `1/T_0`（周期） | 正常 | **仍正常，未被误伤** ✔

### 基线

`prose_math.py` **不属于冻结清单**（它只参与出成品的渲染，不参与校验），
故**不需要重冻基线**。本次未改动任何冻结文件。

---

## 2026-09-19 · 编排器/体检：① 高频判活不再真握手　② 网络抖动不计入连续失败

**改动人**：质检方（质检方）　**授权来源**：需求方「今天要看到初版；能用 Codex 额度就用，
没有就靠你补」（2026-09-19 12:10）

### 背景：12:05 起网络明显劣化，批次 18 连撞三轮

| 阶段 | 战绩 |
|---|---|
| 07:43~09:15 | 第 7~16 批，**12 次尝试成功 10 次** |
| 12:02 | 第 45 轮成功（第 17 批交付） |
| 12:10 / 12:19 / 12:37 | 第 46/47/48 轮**连续失败**，批次 18 未产出 |

失败原文（`轮次_NNN_codex输出.txt`）：
```
[错误] Reconnecting... 4/5 (stream disconnected before completion:
       IO error: peer closed connection without sending TLS close_notify)
```
**两条出口都一样**（轮 43/44 走 7897、轮 46~48 走 10808）。
而同一时刻的短请求基准：**10808 12/12、7897 12/12 全通**。
→ 结论：**短连接正常，长连接（WebSocket）在中途被掐**。

### 改动一 · `net_guard` 增加「只判活」模式，看门狗用它

**起因（质检方自查）**：本轮早先我把 `http_connect_test()` 改成了「真握手」，
而看门狗在对方沉默时会**每 5 秒**调一次 `net_guard.evaluate()`（阈值：沉默 90 秒起判）。
真握手意味着高频往代理里塞真实 TLS 连接 —— 这是我自己引入的额外负载。

**改动**：
| 文件 | 位置 | 改动 |
|---|---|---|
| `net_guard.py` | `http_connect_test()` | 增参 `handshake=True`；为 False 时 CONNECT 通了就返回，不做握手 |
| `net_guard.py` | `evaluate()` | 增参 `handshake=True`，透传给上面 |
| `orchestrator.py` | 看门狗第 ② 条（`idle >= net_dead_limit` 那句） | `evaluate()` → `evaluate(handshake=False)` |

**原则**：**判活只判活，选通道才真握手。**
（`wait_for_net` 里的通道选择仍用真握手 —— 那是唯一能给"这条隧道真能跑数据"背书的方式。）

⚠ **诚实修正**：实测 `handshake=True` 2.82s、`handshake=False` 1.57s ——
**只省了 1.2 秒，不是我原先设想的「0.01s vs 3s」**（代理现在确实会拨上游了）。
所以「我给代理制造了连接风暴」这个判断**被自己的实测削弱了**：
频率是按秒计的几次，不是几十次并发。
→ **本次掉线的主因仍是网络本身**（长连接被掐），改动一只是顺手去掉一份没必要的负载。

### 改动二 · 网络抖动**不计入**「连续失败」

| 文件 | 位置 | 改动 |
|---|---|---|
| `orchestrator.py` | 新增常量 `NET_DROP_KEYS = ("网络重连反复失败", "网络断了")` | 与 `QUOTA_KEYS` 对称 |
| `orchestrator.py` | 主循环失败分支，在 `else` 前插入 `elif any(k in note for k in NET_DROP_KEYS):` | 置「网络抖动」状态后 **`continue`（进下一轮再试）**，不累加连续失败 |

**为什么**：看门狗因长连接被掐而终止一轮时，**Codex 本身没问题**。
把它计入「连续失败」的后果很具体 —— 攒到上限（4）就停机，
此后**每次定时任务只试 1 轮就停**，一个网络抖动期里几乎推不动。
网络该走它自己的路径：**下一轮的「网络体检」会如实判定通不通**，
不通就等、通了就继续（那是原本就设计好的自愈路径）。

> 这与当天早些时候修的「额度用尽」是**同一类**：**环境故障 ≠ 内容失败**。

⚠ **判定词故意不含**「X 分钟没有任何事件输出」和「单轮超时」——
那两种既可能是连接卡死、也可能是对方在长时间生成内容，不能一口咬定是网络。

### 对已有内容的影响

- **对内容与正确性零影响**：不碰 `physkit/**`、不碰 `qc.py` 任何判定；
  改的是「失败怎么归类」与「高频判活的代价」。
- **放宽了什么**：网络抖动时循环会**多试几轮**才退出（原本 4 轮就停）。
  上限仍在：单次运行受 `--rounds 6 --minutes 170` 约束，任务计划另有 3 小时上限。
- **没有放松任何内容检查**：指纹 / 覆盖门槛 / 独立复算 / 成品一致性 / 内容自洽性一字未改。

### 验证

- `python -m py_compile` 两个文件均通过
- 实测两种模式：真握手 → 通（端口 10808，TLS 1.25s）；只判活 → 通（端口 10808）
- `continue` 经确认落在主循环 `for n in range(1, rounds + 1)` 内（第 1313 行起）
- `python work/qc.py --check` → 见「基线」一节

### 基线

改动后已重冻 `work/net_guard.py` 与 `work/orchestrator.py`。
备份：`work/_backup/net_guard_改判活与网络计数前_20260919_1250.py`（sha `85bae131…`）、
`work/_backup/orchestrator_改判活与网络计数前_20260919_1250.py`（sha `d8fe2ff3…`）。

---

## 2026-09-19 · 单位表扩容：补 `nm / eV / keV / MeV / u`（为第 20、21 章开能力）

**改动人**：质检方（质检方）　**授权来源**：需求方「今天要看到初版；能用 Codex 额度就用，
没有就靠你补」（2026-09-19 12:10）

### 为什么必须改：这两章**在机制上产不出来**

第 20 章「波粒二象性与原子结构」和第 21 章「原子核与核能」，
教材一律用 **nm / eV / MeV / u** 表达（普朗克常量、光电效应、德布罗意波长、
原子质量单位、质能方程……）。

而这些单位原先**不在 `physkit/quantity.py` 的 `UNITS` 表里** →
任何一条用它们标注单位的公式，量纲检查都会直接报「不认识这个单位」。

关键在于：`physkit/**` 属**冻结范围**，
`work/批次计划.md` 的交接契约里写死了执行方**不许改**它。

→ 所以这不是「写得好不好的问题」，是**执行方无论如何都写不出来**。
必须由质检方开出这个能力。

### 改了哪个文件、哪几处

只动 `work/physkit/quantity.py` 的 `UNITS` 表，新增一组 6 个键：

| 单位 | 量纲 | 说明 |
|---|---|---|
| `nm` | L | 纳米（波长） |
| `μm` | L | 微米 |
| `eV` | M·L²/T² | 电子伏，量纲同 J |
| `keV` | M·L²/T² | 千电子伏 |
| `MeV` | M·L²/T² | 兆电子伏 |
| `u` | M | 原子质量单位，量纲同 kg |

量纲只记「种类」不记换算系数，故 `1 eV = 1.602×10⁻¹⁹ J` 只需标成 J 的量纲。

### ★ 这是「开能力」，不是「松检查」

- **没有放宽任何一条判定**：所有检查的严格程度一字未改。
  写错单位的公式**照样**会被量纲对不上抓出来 —— 只不过现在校验器多认识几个单位名。
- **没有改动任何已有内容**：`UNITS` 是「单位名 → 量纲」的**查表**，
  新增键不会影响原有键的解析；已交付的 1~17 批一条都没受牵连。
- 与「把 `T_` 改成 `T`」那一条的区别：那条是**显示层**问题（读者看到什么）；
  这条是**能力层**问题（校验器认不认识一个单位）。两者都登记，但性质不同。

### 验证

- `python -m py_compile work/physkit/quantity.py` → 通过
- 逐个解析：`eV/keV/MeV → M·L²·T⁻²`、`u → M`、`nm/μm → L`；
  **恒等自检**：`parse_unit("eV") == parse_unit("J")` → True；
  `parse_unit("u") == parse_unit("kg")` → True
- `python work/validate.py` → **知识点 101 全部通过、校验 1260 项 100% 通过**
  （与改动前完全一致，证明没有副作用）
- `python work/build_site.py` → 成品 1154.0 KB，全部通过
- `python work/qc.py --check` → 指纹 0、独立复算 356 项全符、七项全绿

### 基线

改动后已重冻 `work/physkit/quantity.py`（`7021b54d0e1905e6` → `1b67459a28f42de4`）。
备份：`work/_backup/quantity_加单位前_20260919_1315.py`。

⚠ **本次是被需求方「今天要看到初版」的目标逼出来的**：
如果不是手工排查单位表，这两章会一直卡在「执行方被禁止改校验器」上，
而且表象会是「Codex 写不出来」—— **看起来像模型能力问题，其实是机制没开门**。
这类问题值得记一笔：**冻结边界是必要的，但冻住之后要有人负责「开新门」。**

---

## 2026-09-19 · 【已知限制 · 未修】数值比较的「1.0 下限」会让小量级内容失效

**发现人**：质检方（代产第 21 章时实测撞上）
**状态**：⚠ **本次只记录 + 给出规避办法，故意不改代码**（理由见末尾）

### 现象

核物理的量级在 10⁻¹¹ ~ 10⁻³⁰。把第 21 章按真实量级写出来之后，`validate.py` 报：

```
✘ [不等式关系] 情境「一次裂变」：m_before - m_after = 3e-28，0 = 0，不满足 m_before - m_after > 0
★ 以下错误声称会被抓住、实测却抓不住：  （3 条）
```

`3e-28 > 0` 被判成"相等"。

### 根因（`work/physkit/checks.py` 三处）

| 行 | 代码 | 后果 |
|---|---|---|
| 243 / 265 | `tol = spec.get("tol", 1e-6)`；`if abs(got-ref) <= tol * max(1.0, abs(ref))` | `max(1.0, …)` 让 |ref| < 1 时的容差**固定为 1e-6 绝对** |
| 371 / 404 | `tol = spec.get("tol", 1e-6)`；`if abs(a-b) <= tol * max(1.0, abs(a))` | 同上（跨公式一致） |
| 478 / 505 / 507 | `tol = spec.get("tol", 1e-9)`；`scale = max(abs(a), abs(b), 1.0)` | 同上（不等式关系） |
| `kb.py` 166 | `if abs(a - b) > 1e-6 * scale:` ← **硬编码** | **`trap_test` 不受 `tol` 控制** |

**一句话**：`max(1.0, |值|)` 这个下限，等于给所有小于 1 的量发了一张「免费容差」。
量级低于 1e-6 时，数值检查实际上在比"两个都约等于 0 的数"——
**看着通过，其实没在比**。这比不报更危险。

### 规避办法（已实测有效，**不需要改校验器**）

1. **把场景参数写成宏观量级** —— 首选。核反应按 **1 mol** 计：
   单核 2.7×10⁻¹¹ J → 1 mol 1.62×10¹³ J；单核质量亏损 3×10⁻²⁸ kg → 1 mol 1.8×10⁻⁴ kg。
   全部落在 1e-4 ~ 1e13，默认容差够用，`trap_test` 也能正常抓住。
2. **确实没法放大的量级**（核半径 10⁻¹⁵ m、光子能量 10⁻¹⁹ J）：
   在对应 `numeric` / `consistency` / `compare` 检查项里**显式加 `"tol"`**
   （如 `"tol": 1e-20`）—— 这三个检查都认 `spec.get("tol", …)`。
   ⚠ 但 `trap_test` **不认**，所以这类量级的常见错误请标 `caught_by: "人工审核"`。

> 上述办法已写进 `work/批次计划.md` 第 20 批的注意事项，
> 让执行方不必再踩一遍（**这是代产之外更值钱的部分**）。

### 为什么这次不改代码

把下限从 `1.0` 拿掉、改成相对容差，**在原理上更正确**；但风险是：
现有 1321 项检查里有相当一部分 `ref` 是**按三位有效数字写的**
（它们今天能通过，正是因为 1e-6 那个宽松的绝对容差兜住了取整误差）。
一旦改成严格相对容差，这些**作者写对的检查会集体报红**——
**为了让新内容好写，去动已经验收通过的 1321 项检查，是本末倒置。**

### 后续建议（留给将来）

若日后要真正修，应当：
1. 先写一个**实验性脚本**，对现有全部数值检查**同时用两套容差各算一遍**，
   统计"改成相对容差后会红掉多少项"；
2. 若为 0，再改；若不为 0，**必须逐条核对那些 ref 是否只是取整**，
   确认是取整不精确就补精度，不能一律放松或一律收紧；
3. 改完重冻基线并登记。

**在此之前，按上面的「规避办法」走，不要绕开这条限制去凑数字。**

---

## 2026-09-19 · 渲染层：符号的「教材写法」还原（缩写下标 → 横线 / 中文；δ → Δ）

**改动人**：质检方（质检方）　**授权来源**：需求方原话（2026-09-19 20:13）——
「实际学习中没人会把 v平均 写成 vavg 的，都是 v 上面加个横线……
给人看的那部分，符号重复或者说下标用中文，都比现在好」

### 问题一 · 英文缩写下标直接上屏

符号名是**给机器用的**（要能进表达式、能查表），所以作者写成了
`v_avg` / `T_half` / `m_before` / `F_net` 这类英文缩写。
显示层把这些缩写**原样当下标印出来**，页面上就成了程序员的记号，不是教材的记号。

**规模**：全库共 **76 种**多字母英文缩写下标。

### 问题二（更严重）· `delta_` 与 `Delta_` 大小写混乱

`delta_r` 会被渲染成 **δ**_r，而它的含义是「两列波到达某点的路程差」——
应当是 **Δ**r。**δ 与 Δ 在物理里是两个不同的符号**，这不是风格问题，是**记号错了**。

实测：**16 处 `delta_*` 与 16 处 `Delta_*`**，而 16 处小写的含义
**全部是「变化量 / 增量 / 差」**，没有一处是微小量 δ → 五五开的混乱，不是有意区分。
成品里 δ 出现 **35 处**。

### 改了哪个文件

**只改 `work/prose_math.py` 一处**，新增 `display_mathml()`，并在 `build_site.py` 的
**两个唯一出口**（`mathml_block()` / `mathml_inline()`）上挂钩 —— 公式框与符号表
全部经此，一处覆盖全站。

| 处理 | 规则 | 例 |
|---|---|---|
| 上加横线 | `avg` | `v_avg` → **v̄**（`<mover accent="true">`） |
| 中文下标 | 34 条映射 | `total`→总、`half`→1/2、`before`→前、`after`→后、`net`→合、`ind`→感、`line`→线、`rod`→棒、`send`→送、`molecule`→分子、`abs`→大小、`signed`→代 |
| **去掉下标** | `emf`、`const` | `E_emf` → **E**、`G_const` → **G**（教材本来就不写） |
| 保持原样 | `max`/`min`/`AB`/`BA` 等 | 教材本来就这么写 |
| 中文下标改正体 | 全部 | physkit 会给单字加斜体，中文用斜体不合适 |
| 小写 δ → Δ | 符号基底 | `delta_r` → **Δr** |

### ★ 为什么改显示层而不改内容

**改名要动 `expr` 字段与全部检查项，等于重跑 1463 项校验，风险极大；
而显示层改一处即可全站生效、对校验零影响。**
显示与校验就此解耦：**机器认识 `v_avg`，人看到 v̄。**

### 对已有内容的影响

- **对正确性零影响**：`expr` 字段、`symbols` 的名字、单位一个字没动；
  改的纯粹是「渲染成 MathML 的那一刻」。
- **不影响任何校验结论**：改完 `build_site` 仍是 **1463 项 100% 通过**，
  `qc --check` 指纹 0 / 覆盖门槛 0 / 独立复算 414 项 0 不符 / 成品一致性 0 全绿。
- 正文散文走的是另一个函数（`render()`），**未受影响**（自测 29/29 仍通过）。

### 验证

| 项 | 结果 |
|---|---|
| `prose_math.py` 自测 | **29 / 29 通过** |
| `build_site.py` | 结构 0 错、**物理检查 1463 项 100% 通过**、成品 1338.2 KB |
| 英文缩写下标 | **76 种 → 34 种**（剩余多为 `AB`/`max`/`BA` 这类教材本来就这么写的） |
| 上加横线 | 15 处生效 |
| 中文下标 | 感 19 · 总 22 · 合 18 · 大小 8 · 线 5 · 代 5 · 前/后 各 4 · 棒 4 · 送 4 · 分子 3 |
| **δ（小写）** | **35 → 0**；Δ 66 处；`Δr` 已正确出现 |
| 单字母 / 数字下标 | 不受影响（`F_N`、`v_0` 原样） |

### 基线

`prose_math.py` 与 `build_site.py` **均不属于冻结清单**（只参与出成品的渲染），
**不需要重冻基线**。本次未改动任何冻结文件。

### 待人工确认（需求方是物理专业，这几条该他拍板）

1. 剩余 34 种缩写的映射（`rate`/`push`/`spring`/`rel`/`top`/`dot`/`restore`/`test` 等，各 2 处）
2. 「去掉下标」的规则目前只放了 `emf` / `const`，是否还有该去掉的
3. 小写 δ → Δ 这条是**语义假设**（"本库里 δ 一律表示变化量"），已核对 16/16 成立；
   若日后要表达微小量 δ，需从规则里排除

---

## 2026-09-19 · 同一件事的第二轮：**两条渲染路径**都补齐 + 又挖出两处漏网

**改动人**：质检方　**触发**：需求方明确「不用改底层的，只要让人看的可读性增加就行——
给机器看的可以还是那个」。即**确认「显示与校验解耦」这个方向**，要求继续做透。

### 漏网一：正文走的是**另一条渲染路径**，第一轮没覆盖

第一轮只改了公式框（走 MathML，`display_mathml`）。
而**正文散文走的是 `render()`**（`prose_math`），它没有接映射 ——
实测正文里仍残留 **883 处**英文缩写下标（`total` 83 / `ind` 66 / `cap` 51 / `net` 45 /
**`avg` 44** / `eddy` 40 …），而且正文里的 `delta_r` **还是 δ_r**。

→ 已把同一套规则接进 `render()` 的下标处理（`_do_sub`）：

| 场景 | 处理 |
|---|---|
| `total`/`ind`/`cap`/`line`/`rod`… | → 中文下标 |
| `avg` | → 行内横线（`text-decoration:overline`，与公式框的 MathML mover 视觉一致） |
| `emf`/`const` | → 去掉下标 |
| 基底 δ | → Δ |

**结果：正文残留应映射缩写 883 → 0 处。**

### 漏网二：希腊字母下标不进 `<sub>`

`_RE_SUB` 的**下标字符类只写了 `[A-Za-z0-9中文]`，漏了希腊字母**。
于是 `delta_phi` 走到一半变成 `δ_φ` 就卡住了（φ 不在类里）→
页面上残留 `δ_φ` 这种**半机器写法**，既没成下标、也没变 Δ。

→ 下标字符类补上希腊字母区间（`\u0370-\u03ff`）。

### 漏网三：作者在正文里**直接手写小写 δ**

内容里有「速度的变化量，**δv** = v − v₀」这种写法（不是 `delta_v`，是真 δ 字符），
渲染层按规则表匹配不到。

→ 新增规则：**「δ 紧跟一个字母」→ Δ**（`δv`→Δv、`δφ`→Δφ）。
⚠ **只认这一种**：`δ_φ` 带下标的走 `_do_sub`；单独一个 δ（后面是空格或中文）不动 ——
万一将来要表示微小量 δ 就不会误伤。

### 第二批缩写映射（34 → 4）

先把剩余 34 种**用脚本全部列出来、看过每个符号的实际 desc 再定**，不是照单词硬译：

`absorb`→吸 · `release`→放 · `latent`→潜 · `sensible`→显热 · `attr`→引 · `rep`→斥 ·
`mag`→安 · `restore`→回复 · `spring`→弹 · `common`→共 · `rel`→相对 · `rate`→速率 ·
`decay`→衰变 · `molecular`→分子 · `series`→串 · `shunt`→并 · `test`→试探 · `order`→级 ·
`sat`→饱和 · `dot`→面积 · `perp`→有效 · `sep`→分离 · `oi`→物像 · `top`→顶 ·
`far`→远 · `near`→近 · `inside`→内 · `push`→拉

★ **故意不改的四个**：`AB` / `BA`（`F_AB` 就是「A 对 B 的力」，标准写法）、
`max` / `min`（教材标准）。

### 附带修正：`delta_Ek` → **ΔE_k**

`delta_Ek` 渲染出来是 `Δ_Ek`，但教材写 **ΔE_k**（Δ 挂在 E 上、k 才是下标）。
根源是命名系统只支持一层下标，只能在显示这一步掰回来。

### 验证

| 项 | 结果 |
|---|---|
| `prose_math.py` 自测 | **49 / 49 通过**（原 29 条 + 新增 20 条，含 5 条"不该被误伤"的反向保护） |
| `build_site.py` | 结构 0 错、**物理检查 1463 项 100% 通过**、成品 1338.4 KB |
| `qc.py --check` | 指纹 0 / 覆盖门槛 0 / **独立复算 414 项 0 不符** / 成品一致性 0 / 全绿 |
| 公式框（MathML）残留 | **4 种**：`AB`/`BA`/`max`/`min`（全是教材标准写法） |
| 正文（`<sub>`）残留应映射 | **0 处** |
| 可见文本里的 δ | **35 → 0**；Δ 353 处 |

**已知剩余的 2 处 δ**：在 `data-search` **隐藏属性**里（搜索索引，不显示），
来源是搜索串直接拼接原始 `desc`。影响仅限于"搜 Δv 可能匹配不到这张卡"，
**不是可见问题**，故本轮未动。

### 基线

`prose_math.py` / `build_site.py` 均**不属冻结清单**，不需要重冻。本轮未改动任何冻结文件。
