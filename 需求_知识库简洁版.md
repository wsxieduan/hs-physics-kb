# 需求交接 · 高中物理知识库「简版」系列

> **这份文件写给谁**：执行方（Codex）。
> **谁写的**：质检方（质检方）。需求方说「已经让另一个 agent 做好初期调查，
> 你要做的是等它给你的消息」—— 这份就是那条消息。
> **日期**：2026-09-23　**需求方已确认的方向**：见第三节。

---

## 一、需求方的原话（不要曲解，逐字照抄）

> 「好的，现在这个知识库已经有网站和 HTML 了，但是我还是有些许不满意，
> **因为他不够简洁对于学生和老师**，它可能**更倾向于是一个知识点和符号速查
> 以及常见错误了解**的东西。**至于不重要的代码审查则是另外的。**
> 我希望你整理界面再做个更简洁的版本，我们可以先聊聊该往哪方向进行修改」

**三个要点**：
1. 不满意的地方是「**不够简洁**」，受众是**学生和老师**；
2. 想要的是「**知识点速查 + 符号速查 + 常见错误**」；
3. 「**代码审查（=那 1535 条校验记录）是另外的**」—— 它不该出现在简版里。

---

## 二、现状与「重在哪」（已量化，别猜）

现有成品：`outputs/高中物理知识库.html`，**1630 KB**，**123 个知识点**。

| 事实 | 数字 |
|---|---|
| 单条校验记录 | **1535 条** |
| 校验明细占卡片篇幅 | **37%**（472 KB） |
| 每张卡片的小标题 | **7 个**（定义 / 物理意义 / 符号 / 公式 / 推导要点 / 常见错误 / 例题） |
| **单张卡片的信息片段** | **182 个** |

→ **结论**：学生要的三样（知识点 / 符号 / 常见错误）被埋在一堆工程证据里。
这就是「不简洁」的确切来源。

---

## 三、需求方已经拍板的三条（不要再问）

1. **不是做"一个简洁版"，而是做"多个版本"** —— 老师版 / 学生版 / 简洁版等，面向不同读者。
2. **常见错误挂在知识点下面**（不单独做总览页）：每条知识点头上挂 1~2 条「最容易错」，
   长在公式旁边。
3. **1535 条校验记录全部移出简版**：现在这版 `高中物理知识库.html` 原封不动保留为
   「工程版」，两个产品分工明确。

---

## 四、架构要求：**一个内容源，多个出口**（这条是硬要求）

**绝对不要**为每个版本复制一份内容 —— 那样以后改一个公式要改四遍，迟早不一致。

正确做法：

```
work/kb/*.json          ← 内容源：只有这一份，谁都不许复制
        │
        ├─ build_site.py     → outputs/高中物理知识库.html        （工程版，已存在）
        ├─ build_lite.py     → outputs/高中物理知识库-学生版.html  （学生版，已存在，见第五节）
        └─ （待办）           → 老师版 / 简洁版 …
```

**版本之间的差别只是"给不同的人看不同的部分"**，不是"不同的内容"。
这和这个项目一贯的原则是同一个：**显示与数据解耦**（符号显示那一轮已经验证过：
改显示层对校验结论零影响）。

---

## 五、已有的参考实现（**可以直接用、可以改进、也可以推翻重写**）

质检方已经做出一个**学生版原型**并实测过，代码在 `work/build_lite.py`：

| 项 | 实测值 |
|---|---|
| 产物 | `outputs/高中物理知识库-学生版.html` |
| 体积 | **604 KB**（工程版的 **37%**） |
| 知识点 | **123 张卡片**（一个不少） |
| 公式 | **251 条** |
| 校验记录 | **0 条**（刻意不带） |
| 外部依赖 | **0**（单文件、断网可用） |
| 视图 | 按知识点 / 全部公式 / 符号速查（三个按钮切换） |

**它的卡片结构**（就是需求方要的那种"一眼看完"）：

```
知识点标题
一句话说清是什么          ← 取定义的首句
核心公式（全部）＋ 适用条件
▍最容易错                ← 优先挑「量纲抓不住、只能靠数值代入暴露」的那类错误
▸ 看一道例题（默认收起）
▸ 看符号表（默认收起）
```

**两个实现细节值得保留**（都是踩过的坑）：

1. **卡片里的符号表不用 MathML，用文字形式的教材写法**。
   MathML 很啰嗦，一个符号几十字节；123 张卡片各带一份符号表，
   光这一项就多 50 KB 以上。而「符号速查」视图里已经有完整的数学排版了。
   （实测：改用文字写法后 657 KB → 604 KB）
2. **`fmt` 要抹掉浮点噪声**：`cos(π/2)` 在计算机里算出来是 `3.7e-16` 而不是 0，
   直接显示会让人以为程序算错了。小于 1e-9 一律当 0 处理。

**还剩下的优化空间**（604 KB → 目标 400 KB 以内）：
「全部公式」视图把 251 条公式的 MathML **又存了一遍**（卡片里已有一份）。
可以考虑改成运行时用 JS 从卡片里收集，而不是构建时重复输出。

---

## 六、验收标准（可机检，逐条打勾）

做完必须全部满足，缺一条算没做完：

- [ ] **单文件**：双击就能打开；**外部依赖 = 0**（不许引 CDN、不许引字体）
- [ ] **断网可用**
- [ ] **知识点数 = 123**（一个都不能少）
- [ ] **公式数 = 251**
- [ ] **校验记录数 = 0**（搜 `class="chk ` 应为 0 处）
- [ ] **每条知识点至少挂 1 条「最容易错」**
- [ ] **手机能正常看**（窄屏不横向溢出）
- [ ] 体积**报告实测值**（当前基线：学生版 604 KB / 工程版 1630 KB）
- [ ] 内容源 `work/kb/*.json` **一个字都没改**（改内容要另开任务，不和这个混）

---

## 七、明确不要做的

| 不要做 | 原因 |
|---|---|
| 改 `work/kb/*.json` 的内容 | 这一版只动"怎么渲染"，不动"写什么" |
| 动 `work/physkit/**`、`qc.py`、`net_guard.py`、`orchestrator.py` | 冻结文件；且这版不涉及校验 |
| 为每个版本复制一份内容 | 见第四节，会埋下不一致的隐患 |
| 把校验记录"藏起来"而不是移出 | 藏起来体积还是那么大，需求方要的是**真的轻** |
| 一口气做四个版本 | 先做**一个**（学生版）做扎实，需求方看过再加 |

---

## 八、怎么开始

1. 先跑一遍现有的，把基线记下来：
   ```
   python work/validate.py          # 应仍是 1535 项 100% 通过
   python work/build_site.py        # 工程版，约 1630 KB
   python work/build_lite.py        # 学生版原型，约 604 KB
   ```
2. 打开 `outputs/高中物理知识库-学生版.html` 看效果（双击即可）
3. 决定路线：
   - **改进原型**（推荐，省事）→ 直接改 `work/build_lite.py`
   - **另起一个 `build_teacher.py`**（做老师版）→ 可大量复用 `build_lite.py` 的模板
4. 做完把实测数据（体积 / 条目数 / 依赖数）报给需求方

---

## 九、一句提醒

需求方这一版要的是「**给学生的东西**」，不是「给工程师看的东西」。
**判断标准不是"功能多不多"，而是"学生扫一眼能不能用上"。**
拿不准的时候，宁可再砍一块。
